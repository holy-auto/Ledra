-- ============================================================
-- 本番の schema_migrations に記録されているバージョンに対応する**空ファイル**。
--
-- 経緯: この変更は Supabase MCP の apply_migration で本番へ直接適用した。
-- その方式は本番の schema_migrations に「Supabase が採番したバージョン」を
-- 記録するだけで、リポジトリにファイルを作らない。結果、本番にだけ存在する
-- バージョンが生まれ、`supabase db push` が
--   "Remote migration versions not found in local migrations directory."
-- で停止した（2026-08-25、run 32909421793）。停止するとこれ以降のスキーマ変更が
-- 本番に届かなくなるため、**本番のバージョンには必ず同名のファイルを置く**。
--
-- SQL の中身はここには書かない。実体は 20260824010000_revoke_anon_round2.sql が持っており、
-- そちらは本番では未適用として残っている（内容は冪等なので、後で適用されても
-- 二重には効かない）。空DBからの再生でも 20260824010000_revoke_anon_round2.sql が実行されるので、
-- このファイルが無くても再生結果は変わらない。
-- ============================================================
